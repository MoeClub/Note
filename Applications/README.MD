
# Install
```
tar -zxvf "Application.tar.gz" -C "/Applications"
```

# nPlayer
```
curl -sSL -o "nPlayer.app.tar.gz" "https://raw.githubusercontent.com/MoeClub/Note/master/Applications/nPlayer.app.tar.gz"
tar -zxvf "nPlayer.app.tar.gz" -C "/Applications"
```
